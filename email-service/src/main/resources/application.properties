# Server Configuration
server.port=8081
spring.application.name=email-service

# Oracle Database Configuration
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/XEPDB1
spring.datasource.username=system
spring.datasource.password=sachi2006
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# Eureka Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Mailjet Configuration
mailjet.api.key=001fd9c5bf9a36d301db897d73e8b24d
mailjet.api.secret=510a6f396dfb655fed4cc378dc720545
mailjet.sender.email=sachithasamadhi1@gmail.com
mailjet.sender.name=Urbanfood

# Email Scheduler Configuration
email.scheduler.fixed-rate=60000
email.scheduler.batch-size=50

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.com.urbanfood=DEBUG

#For testing
spring.cloud.compatibility-verifier.enabled=false
eureka.client.enabled=false